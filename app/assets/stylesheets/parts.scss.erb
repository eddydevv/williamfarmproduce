/* nav */

nav {
  a {
    font-family: $ubuntu;
    text-transform: uppercase;
  }
  a:link, a:visited, a:hover, a:active{
  }
}

/* inner wrapper */

.inner-wrapper {
  margin-top: $navbar-height + $navbar-margin-bottom;
}

/* header */
header{
  font-family: $arima_madurai;
  color: $magic_mint;
  background-color: rgba($raisin_black, 0.4);
  padding: 10px 20px;
}

section {
  min-height: 72vh;
  background-color: rgba($raisin_black, 0.4);
  padding: 20px;
}
