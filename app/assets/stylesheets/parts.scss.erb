/* nav */

nav {
  .navbar-brand {
    font-family: $handlee;
    font-size: 1.6em;
    border-left: 1px solid rgba($light_yellow, 0.2);
  }
  a {
    font-family: $arima_madurai;
    text-transform: uppercase;
    font-size: 1.4em;
    border-right: 1px solid rgba($light_yellow, 0.2);
  }
  a:link, a:visited, a:hover, a:active{
  }
}

/* inner wrapper */

.inner-wrapper {
}

/* header */
header{
  font-family: $arima_madurai;
  color: $magic_mint;
  background-color: rgba($raisin_black, 0.4);
  padding: 10px 20px;
}

section {
  min-height: 75vh;
  background-color: rgba($raisin_black, 0.4);
  padding: 20px;
}
